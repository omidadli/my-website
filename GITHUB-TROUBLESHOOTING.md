# راهنمای جامع عیب‌یابی اتصال به GitHub و تنظیم کلیدهای SSH

این راهنما و اسکریپت تشخیصی همراه آن به شما کمک می‌کند تا وضعیت Git، ارتباط با GitHub، مجوزهای دسترسی و احراز هویت (SSH / HTTPS) را گام‌به‌گام بررسی و خطای Push را برطرف کنید.

---

## ۱. اجرای اسکریپت عیب‌یابی سریع

یک اسکریپت خودکار در ریشه پروژه قرار دارد که وضعیت مخزن، کانفیگ‌ها، ریموت‌ها و ارتباط شبکه را بررسی می‌کند:

```bash
./check-github.sh
```

این اسکریپت موارد زیر را بررسی می‌کند:
1. نصب بودن و نسخه Git
2. وضعیت مخزن، شاخه فعال (`main`) و تعداد کامیت‌ها
3. نام و ایمیل کاربر (`user.name` و `user.email`)
4. آدرس ریموت مخزن (`git remote -v`)
5. دسترسی شبکه به API گیت‌هاب از طریق HTTPS
6. وجود کلیدهای SSH و تست احراز هویت با سرور گیت‌هاب

---

## ۲. بررسی و تنظیم Remote Origin

اگر مخزن شما هنوز آدرس ریموت ندارد یا آدرس اشتباهی دارد:

### مشاهده ریموت‌های فعلی:
```bash
git remote -v
```

### افزودن ریموت جدید (حالت HTTPS با توکن):
```bash
git remote add origin https://github.com/omidadli/<نام-ریپازیتوری>.git
```

### افزودن ریموت جدید (حالت SSH):
```bash
git remote add origin git@github.com:omidadli/<نام-ریپازیتوری>.git
```

*(اگر قبلاً وجود دارد ولی می‌خواهید آدرس را تغییر دهید: `git remote set-url origin <URL>`)*

---

## ۳. راهنمای تنظیم کلید SSH (در صورت عدم موفقیت HTTPS)

استفاده از کلید SSH پایدارترین و امن‌ترین روش برای ارتباط با گیت‌هاب است و محدودیت‌های توکن و انقضای رمز عبور را ندارد.

### گام اول: ساخت کلید SSH اختصاصی (الگوریتم پرسرعت و امن Ed25519)
در ترمینال سیستم خود دستور زیر را وارد کنید (ایمیل خود را قرار دهید):

```bash
ssh-keygen -t ed25519 -C "omidadli78@gmail.com" -f ~/.ssh/id_ed25519 -N ""
```

### گام دوم: کپی کردن کلید عمومی (Public Key)
محتوای کلید عمومی ساخته‌شده را کپی کنید:

```bash
cat ~/.ssh/id_ed25519.pub
```
*(متنی که با `ssh-ed25519` شروع می‌شود و به ایمیل شما ختم می‌شود را به طور کامل کپی کنید).*

### گام سوم: افزودن کلید به حساب گیت‌هاب
1. در مرورگر به آدرس زیر بروید:  
   **[github.com/settings/keys](https://github.com/settings/keys)**
2. روی دکمه سبز رنگ **New SSH key** کلیک کنید.
3. در فیلد **Title** یک نام دلخواه بگذارید (مثلاً `Work Laptop` یا `AI Studio Dev`).
4. فیلد **Key type** را روی **Authentication Key** نگه دارید.
5. کلید کپی‌شده را در کادر **Key** قرار دهید و **Add SSH key** را بزنید.

### گام چهارم: تست اتصال به گیت‌هاب از طریق SSH
دستور زیر را در ترمینال اجرا کنید:

```bash
ssh -T git@github.com
```
در پاسخ باید پیامی مشابه زیر دریافت کنید:
> `Hi omidadli! You've successfully authenticated, but GitHub does not provide shell access.`

---

## ۴. ارسال تغییرات (Git Push)

پس از تنظیم SSH یا HTTPS:

```bash
# اطمینان از نامگذاری شاخه اصلی به main
git branch -M main

# پوش نهایی
git push -u origin main
```

*(در صورتی که در ریپازیتوری گیت‌هاب خطای Non-fast-forward دریافت کردید، با دستور `git push -u origin main --force` کامیت‌ها را همگام کنید).*
