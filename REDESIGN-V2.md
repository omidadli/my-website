# بازطراحی V2 — سیستم طراحی جدید (دموی صفحه اصلی)

الهام‌گرفته از سبک **webild.io** ولی شخصی‌سازی‌شده برای برند شخصی امید عدلی: روشن، هوا‌دار، حرفه‌ای و متمرکز بر تبدیل.

## اصول طراحی
1. **روشن و هوا‌دار:** بوم `#f6f6f4` با گرادیان‌های پاستلی بسیار ملایم (بنفش/آسمانی/نعناعی) به‌جای نئون‌های تیره.
2. **یک رنگ اکسنت:** ایندیگو `#4f46e5` فقط برای اقدام‌ها و نکات کلیدی؛ بقیه رابط خنثی است.
3. **سلسله‌مراتب تایپوگرافی:** تیترهای نمایشی `font-black` با اندازه بزرگ فقط برای H1/H2؛ متن بدنه خاکستری ملایم.
4. **کارت‌های گرد و سایه نرم:** شعاع ۲۴–۴۴px، سایه‌های پخش‌شده کم‌عمق، حاشیه‌های ۸٪ مشکی.
5. **شیشه فقط جا‌هایی که لازم است:** نویگیشن شناور، داک اقدام سریع و چیپ‌های شناور.
6. **ریتم و فضای تنفس:** سکشن‌ها با `py-14→20` و حداکثر عرض `6xl`.

## توکن‌ها (src/index.css — لایه components)
- `--nd-bg / --nd-surface / --nd-ink / --nd-muted / --nd-faint`
- `--nd-accent(-strong/-soft)` ، `--nd-sky-soft` ، `--nd-mint-soft` ، `--nd-peach-soft`
- کلاس‌ها: `nd-card` ، `nd-card-hover` ، `nd-glass` ، `nd-panel` ، `nd-eyebrow` ، `nd-h1/h2` ، `nd-btn(-accent/-ghost)` ، `nd-chip` ، `nd-marquee` ، `nd-blob`

## چه چیزی حفظ شد
- تمام محتوا و نوشته‌ها از `src/data/content.ts` و Context (بدون تغییر).
- ساختار سکشن‌های CMS (`PAGE_SECTIONS.home`) و `SectionWrapper` → ترتیب/مخفی‌سازی سکشن‌ها از ادمین همچنان کار می‌کند.
- `EditableText` روی آمار و فوتر، `RepeaterControls`/`SectionEditHeader` در سکشن آمار.
- المان‌های اضافه به شکل پالایش‌یافته: اسپلش (سریع‌تر و روشن)، کرسر سفارشی (ظریف با mix-blend)، داک شناور (شیشه‌ای)، بلاب‌ها (پاستلی)، داشبورد ایزومتریک سه‌بعدی (داخل فریم مرورگر هیرو)، TiltCard روی کارت‌های مسیر.

## فازهای بعدی (پس از تایید دمو)
1. بازطراحی بقیه صفحات با همین سیستم (خدمات، نمونه‌کارها، درباره، بلاگ، تماس، پروژه‌ها، محصولات).
2. ساده‌سازی UI پنل ادمین/CMS (نوار ابزار مینیمال، ویرایش درجا یکپارچه).
3. بازبینی حالت موبایل و دسترس‌پذیری (کنتراست، فوکوس، reduced-motion).

---

# V2.1 — استراتژی مشاوره‌ای و لایه سینمایی (هیبرید)

## تصمیم مشاوره‌ای
کاربر بین سبک‌ها مردد بود؛ تصمیم نهایی من به‌عنوان مشاور برند/CRO:
**هیبرید سینمایی** — صحنه‌های تیره براق (nd-stage) برای لحظات احساسی و تصمیم‌گیری
(هیرو، اثبات، CTA نهایی) و بدنه روشن آرام برای مطالعه. تضاد تیره/روشن = درام + راحتی.
استایل براق سه‌بعدی (رفرنس تصویر سوییچ) در فریم داشبورد، گلس‌ها و لبه‌های نئونی اعمال شده.

## بازچینی قیف محتوا (CRO: اول اثبات، بعد پیشنهاد)
ترتیب جدید صفحه اصلی:
1. HERO — صحنه تیره: قول برند + prompt-box + فریم داشبورد + کارت معرفی روی لبه
2. TRUST_BAR — مارکی نام برندها (اعتماد انتقالی)
3. PATH_NAV — خودتفکیکی مخاطب
4. PROOF — صحنه تیره اثبات: آمار + ۲ کیس منتخب (داده قبل از پیشنهاد)
5. SERVICES_TABS — خدمات به‌عنوان راه‌حل + لنگر قیمت در صفحه خدمات
6. HOW_I_WORK — کاهش ریسک ذهنی
7. WHY_OMID — تمایز + نقل‌قول مشتری
8. INSIGHTS — لید مگنت «آنالیز رایگان» (پله تبدیل سبک) + نوشت‌های تازه (هدف ترافیک بلاگ)
9. FAQ — پاسخ به اعتراض‌ها (قیمت/تضمین/ریموت/تفاوت با آژانس/زمان نتیجه)
10. FINAL_CTA — صحنه تیره پایانی
* تایم‌لاین رزومه از خانه حذف شد (در صفحه «درباره من» هست) — خریدار به نتیجه اهمیت می‌دهد نه سابقه.

## لایه مدرن/سینمایی
- MaskLines: ریویل ماسکی خط‌به‌خط تیترها
- Magnetic: دکمه‌های مغناطیسی
- ScrollProgress: نوار پیشرفت مطالعه بالای صفحه
- Grain: بافت نویز فیلمیک ۵٪ روی کل سایت
- nd-stage / nd-glass-dark / nd-hairline-top: استیج‌های تیره براق با نور لبه‌ای
- Aurora orbs با درift آرام؛ احترام کامل به prefers-reduced-motion
- همه انیمیشن‌ها transform/opacity برای ۶۰fps

## دارایی‌های در انتظار تصمیم کاربر
- عکس پرتره حرفه‌ای → اسکن «داستان من» (جای خالی در نظر گرفته شده)
- ویدیوی معرفی ۳۰–۶۰ ثانیه → قابل جایگذاری در هیرو
- لوگوی مشتریان → ارتقای TRUST_BAR از متن به لوگو
- سوییچ تم لوکس روز/شب → فاز ۲ در صورت درخواست
