# راهنمای اتصال CMS به Cloudflare (D1 + R2)

این سایت حالا یک API کامل روی **Cloudflare Pages Functions** دارد:

| مسیر | کار | دسترسی |
|---|---|---|
| `GET /api/content` | خواندن کل محتوای سایت از D1 | عمومی (کش ۳۰ ثانیه) |
| `PUT /api/content` | ذخیره‌ی کل محتوا در D1 | فقط ادمین |
| `POST /api/auth` | ورود با نام کاربری + رمز عبور | عمومی (با محدودیت تلاش ناموفق) |
| `GET /api/auth` | اعتبارسنجی جلسه‌ی ادمین | با توکن |
| `GET /api/health` | تشخیص زنده‌ی سرویس ورود (کدام Secret نرسیده، قفل IP، بایندینگ D1) — فقط نام‌ها و true/false، هرگز مقدار رمز | عمومی |
| `GET /api/media` | لیست کتابخانه رسانه | عمومی |
| `POST /api/media` | آپلود فایل در R2 | فقط ادمین |
| `DELETE /api/media?key=…` | حذف فایل از R2 | فقط ادمین |
| `GET /api/media/file/<key>` | دریافت فایل از R2 | عمومی (کش طولانی) |
| `POST /api/slug` | ساخت اسلاگ انگلیسی از عنوان فارسی | فقط ادمین |
| `GET/POST /api/comments` | دیدگاه‌های بازدیدکنندگان (صف تایید) | POST عمومی / GET فقط تاییدشده‌ها |
| `PATCH/DELETE /api/comments` | تایید، پاسخ و حذف دیدگاه | فقط ادمین |
| `POST /api/chat` | دستیار هوشمند AI (پاسخ از داده‌های سایت) | عمومی (۱۵ پیام/ساعت/IP) |
| `GET /api/chat` | تاریخچه گفتگوها برای پایش رفتار | فقط ادمین |

## قدم ۱ — wrangler.toml (انجام شد ✅)

دیتابیس D1 در فایل `wrangler.toml` وارد شده است:
- نام: `omidadli01-site-db`
- شناسه: `89b0d5f8-7ded-4dc8-9f52-79befd0abbec`

**R2 اختیاری است — کارت بین‌المللی لازم نیست!**
سیستم رسانه به‌صورت هوشمند دو حالته است:
- **بدون R2 (پیش‌فرض فعلی):** فایل‌ها پس از فشرده‌سازی خودکار در خودِ **D1** ذخیره می‌شوند (کاملاً رایگان، همان دیتابیسی که دارید). هر فایل تا ~۱.۴MB؛ تصاویر به‌طور خودکار در مرورگر به حداکثر ۱۶۰۰ پیکسل و کیفیت بهینه کوچک می‌شوند، پس عملاً هیچ‌وقت به این سقف نمی‌رسید. کل فضای رایگان D1 (۵ گیگابایت) برای هزاران عکس فشرده کافی است.
- **با R2 (هر وقت خواستید):** اگر روزی R2 را فعال کردید، فقط binding `MEDIA` را اضافه کنید — کد به‌طور خودکار مسیر R2 را ترجیح می‌دهد و فایل‌های قبلی در D1 هم سرو می‌شوند. هیچ مهاجرتی لازم نیست.

فعال‌سازی R2 در آینده (اختیاری): داشبورد → **R2 Object Storage** → **Activate** → `npx wrangler r2 bucket create omidadli01-media` → بخش کامنت‌شده `[[r2_buckets]]` در `wrangler.toml` را باز کنید → دیپلوی مجدد.

## قدم ۲ — ساخت جدول‌های دیتابیس

```bash
npx wrangler d1 execute <DATABASE_NAME> --remote --file=./schema.sql
```

> جدول‌های «ابزارهای هوشمند» (`tool_access`، `tool_messages`، `tool_settings` و `tool_trials`) هم داخل `schema.sql` هستند و علاوه بر آن، API در صورت نبودشان به‌صورت خودکار می‌سازدشان (شاملِ افزودنِ ستونِ `message_quota` به `tool_access`). اگر دیتابیس از قبل ساخته شده، مایگریشن‌ها را اجرا کنید:
>
> ```bash
> npx wrangler d1 execute <DATABASE_NAME> --remote --file=./migrations/0003_ai_tool_access.sql
> npx wrangler d1 execute <DATABASE_NAME> --remote --file=./migrations/0004_tool_plans_trials.sql
> ```
>
> **قیمت‌گذاریِ پلکانی و بازی‌وارسازی:** هر ابزار سه پلن بر اساس میزانِ مصرف دارد (پایه / حرفه‌ای / VIP) که در `lib/toolPlans.ts` تعریف شده و همه از ۴۰۰٬۰۰۰ تومان به بالا هستند؛ برچسبِ قیمتِ هر پلن از پنل ادمین قابل بازنویسی است (`AI_TOOLS_CONFIG.tools[id].planPrices`). هر پلن یک «سهمیه‌ی پیام» (`message_quota`) و مدت اعتبار دارد، پس کاربرِ پرمصرف و کم‌مصرف یکسان پرداخت نمی‌کنند. برای جذبِ کاربر، هر ابزار **N پیام رایگانِ آزمایشی** دارد که بر پایه‌ی شناسه‌ی دستگاه در جدول `tool_trials` شمرده می‌شود؛ تعدادش از `AI_TOOLS_CONFIG.freeTrialCount` تنظیم می‌شود (۰ = بدون تست، کاملاً قفل).

## قدم ۳ — تعریف Secrets (مهم‌ترین بخش امنیتی)

```bash
npx wrangler pages secret put ADMIN_USERNAME   # نام کاربری دلخواه شما
npx wrangler pages secret put ADMIN_PASSWORD   # رمز عبور قوی (حداقل ۱۶ کاراکتر پیشنهاد می‌شود)
npx wrangler pages secret put AUTH_SECRET      # خروجی: openssl rand -hex 32
npx wrangler pages secret put GEMINI_API_KEY   # برای هوش مصنوعی: دستیار سایت، ابزارهای هوشمند، و ساخت اسلاگ
```

> اگر `GEMINI_API_KEY` نگذارید، اسلاگ‌ها به‌صورت فینگلیش ساخته می‌شوند (مثلاً `afzayesh-narkh-tabdil`) و همیشه دستی قابل ویرایش‌اند.
>
> ⚠️ **سکرت‌ها در زمان دیپلوی به بیلد تزریق می‌شوند:** اگر سکرتی را در داشبورد اضافه یا
> تغییر دادی، تا وقتی دیپلویِ جدید نزنی نسخه‌ی در حال سرویس آن را نمی‌بیند و ورود با پیام
> «نام کاربری یا رمز عبور اشتباه است» رد می‌شود (در حالی که سرور در واقع `503` برمی‌گرداند).
> ترتیب درست: `pages secret put` → `pages deploy`. چون این پروژه `wrangler.toml` دارد، در
> داشبورد هم نوع **Secret (encrypted)** را انتخاب کن (متغیر متنی با دیپلوی پاک می‌شود).
> اگر از `echo "…" | wrangler pages secret put` استفاده کردی، یک newline به رمز اضافه شده؛
> به‌جایش `printf '%s' '…' | …` بزن. تشخیص فوری: `https://omidadli01.site/api/health` و
> راهنمای کامل: [docs/LOGIN-TROUBLESHOOTING.md](./docs/LOGIN-TROUBLESHOOTING.md).
>
> **ابزارهای هوشمند (محصولات):** برای پاسخ‌های واقعی هوش مصنوعی، `GEMINI_API_KEY` لازم است؛ بدون آن ابزارها با یک موتور نمونه‌ی محلی کار می‌کنند. توکن‌های دسترسی این ابزارها با `AUTH_SECRET` امضا می‌شوند، پس تعریف `AUTH_SECRET` (که برای ورود ادمین هم لازم است) ضروری است.

## قدم ۴ — Bindings (اگر با Git یکپارچه دیپلوی می‌کنید)

در داشبورد Cloudflare: **Workers & Pages → پروژه‌ی شما → Settings → Functions → Bindings**:
- D1 database → binding name: `DB`
- R2 bucket → binding name: `MEDIA`

(اگر با `wrangler pages deploy` دیپلوی می‌کنید، همان `wrangler.toml` کافی است.)

## قدم ۵ — دیپلوی

```bash
npm run build
npx wrangler pages deploy dist
```

## توسعه‌ی محلی

```bash
npm run build
npx wrangler pages dev dist --port 8788   # ترمینال ۱ (API + دیتابیس محلی)
npm run dev                               # ترمینال ۲ (Vite — درخواست‌های /api را به 8788 پروکسی می‌کند)
```

اگر API محلی در دسترس نباشد، سایت به‌طور خودکار به **حالت محلی (localStorage)** برمی‌گردد و ورود ادمین با همان رمز محلی قدیمی ممکن است — این حالت فقط برای توسعه است و در نسخه‌ی دیپلوی‌شده، ورود فقط از طریق نام کاربری/رمز عبور ابری انجام می‌شود.

## لایه‌های امنیتی فعال

- رمز عبور و نام کاربری فقط در Secrets کلودفلر (هرگز داخل کد یا ریپازیتوری).
- توکن جلسه: امضای HMAC-SHA256 با انقضای ۷ روزه.
- محدودیت نرخ ورود: بیش از ۸ تلاش ناموفق از یک IP → ۱۵ دقیقه قفل.
- مقایسه‌ی رمز به‌صورت timing-safe.
- آپلود رسانه: فقط ادمین، حداکثر ۱۰MB، فقط فرمت‌های مجاز (تصویر/PDF)، کلید تصادفی‌سازی‌شده در R2.
- فایل‌های SVG با هدر CSP خنثی‌سازی می‌شوند.
- دستیار هوشمند: محدودیت ۱۵ پیام در ساعت برای هر IP؛ ورودی حداکثر ۱۲۰۰ کاراکتر؛ پاسخ فقط از داده‌های خود سایت (System Prompt قابل مدیریت در پنل)؛ تاریخچه کامل گفتگوها در D1 برای پایش رفتار بازدیدکنندگان.
- ابزارهای هوشمند فروشی (تراپیست بیزینسی، مسیرساز، راه‌حل‌یاب، مشتری فرضی): دسترسی «به‌ازای شماره موبایل» توسط ادمین صادر می‌شود و با یک «کد دسترسی» مخفی همراه است. هر دسترسی روی تعداد محدودی «دستگاه» قفل می‌شود تا کد قابل اشتراک‌گذاری رایگان نباشد. توکن نشست HMAC-SHA256 و دارای انقضاست و هر پیام دوباره اعتبارسنجی می‌شود (فعال بودن، عدم‌انقضا، مجاز بودن دستگاه)؛ محدودیت ۶۰ پیام در ساعت برای هر شماره. ادمین می‌تواند دسترسی را لغو یا دستگاه‌ها را آزاد کند.
- مدیریت رفتار هر ابزار (شخصیت/System Prompt، پیام خوش‌آمد، سوال‌های پیشنهادی، دما، اتصال به دیتای سایت) از پنل ادمین قابل ویرایش است و در محتوای سایت ذخیره می‌شود.
- مدیریت توکن API هر ابزار جداگانه: کلید API (Gemini یا سازگار با OpenAI) در جدول امنِ `tool_settings` در D1 ذخیره می‌شود، فقط برای ادمین قابل تنظیم است و **هرگز به‌صورت کامل به مرورگر برنمی‌گردد** (فقط نمایش ماسک‌شده مثل `sk-t••••CDEF`). اگر ابزاری کلید اختصاصی نداشته باشد، به‌صورت خودکار از `GEMINI_API_KEY` سراسری استفاده می‌کند و اگر آن هم نباشد، پاسخ‌های نمونه‌ی محلی برمی‌گرداند.
- دیدگاه‌های بازدیدکنندگان: ذخیره مستقیم در جدول `comments` در D1 (نه مرورگر)، با محدودیت ۵ دیدگاه در ساعت برای هر IP، بررسی اعتبار ایمیل و honeypot ضدربات؛ انتشار فقط پس از تایید شما؛ ایمیل بازدیدکننده هرگز در پاسخ عمومی ارسال نمی‌شود.
- هدرهای امنیتی سراسری در `public/_headers` (nosniff, frame-options, referrer-policy).
